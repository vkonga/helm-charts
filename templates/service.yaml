apiVersion: v1
kind: Service
metadata:
  name: nginx-lb
  labels:
    project: amazon-clone
spec:
  type: LoadBalancer
  selector:
    project: amazon-clone
    app: nginx
  ports:
    - protocol: TCP
      port: 80
      targetPort: {{ .Values.service.targetPort}}
  